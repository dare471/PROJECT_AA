import { createSelect, type Option } from './model'
import { type ReactSelectProps, Select } from './ui'

export { type Option, type ReactSelectProps }

export const SelectFactory = {
	Select,
	createSelect,
}
