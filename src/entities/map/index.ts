import { clearAllCirclesFx, fitBoundsFx } from './events'
import { createMap } from './model'
import { Map } from './ui'

export const MapFactory = {
	Map,
	createMap,
	fitBoundsFx,
	clearAllCirclesFx,
}
