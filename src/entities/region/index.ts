import { createRegions, createRegionsAnalyticsByYear, createRegionsOverlay } from './model'
import { RegionAnalyticCard, RegionsAnalyticsByYear } from './ui'

export { createRegionsOverlay, createRegions, RegionAnalyticCard }
export const RegionsAnalyticsByYearFactory = {
	RegionsAnalyticsByYear,
	createRegionsAnalyticsByYear,
}
